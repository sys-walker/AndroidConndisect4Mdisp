<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eps.connect4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserInterface.MailSender"></activity>
        <activity android:name=".Connect4Logical.Connect4HostActivity" />
        <activity android:name=".UserInterface.HelpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserInterface.DetalleActivity" />
        <activity android:name=".UserInterface.QueryActivity" />
        <activity android:name=".UserInterface.AjustesActivity" />


    </application>

</manifest>